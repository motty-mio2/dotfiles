{{ if and (eq .chezmoi.os "linux") (lookPath "kanata_linux_x64") -}}
[Unit]
Description=Kanata Keymapper

[Service]
ExecStart={{ lookPath "kanata_linux_x64" }} --port 7070
ExecStop=/bin/kill ${MAINPID}
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
{{ end }}
