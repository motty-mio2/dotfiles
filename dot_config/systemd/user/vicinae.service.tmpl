[Unit]
Description=Kanata Keymapper

[Service]
ExecStart={{- joinPath .chezmoi.homeDir "/.local/bin/vicinae" }} server
ExecStop=/bin/kill ${MAINPID}
Restart=always
RestartSec=3

[Install]
WantedBy=default.target

